# note-1.md

1. router.ex 中为什么会存在字符串匹配的路由和词法解析？ 
已处理，临时阶段

2. 现在的产品定位是中文群体，但是软件相关输出都是英文

3. 需要有一个完整的项目日志规划，包括目录位置，特别是对LLM之间的交互日志，这个日子能够指导工作台的流程优化，包括工作台流程、prompt的具体优化
4. 现在工作台对于未知指令的处理还有缺陷
5. 项目目前对于，以对话为主的实现，还是有偏差，理想中的是对话流为主，然后基于对话的信息，让工作台工作
6. 现在工作台重新打开，输入内容，会重新开始，而不是从上次的结束位置开始

7. 开始规划按钮，硬编码了 “我想规划一部 10 万字长篇小说，请生成章节大纲”，需要修改
8. 续写的章节里出现了“场景 2”、“场景 3”这样的字眼，需要分析续写逻辑中是否存在问题，或者是续写的 prompt 设计不合理，以及相应结果是否需要进行后续处理

9. 角色类型的定义和使用还不够清晰，尤其是对于“主角”这个角色类型的定义和使用，需要进一步明确和规范化
10. 请基于当前作品背景、设定和已有角色，和我一起设计一个新角色。结果返回了两个角色候选，但是只有一个待采纳按钮，需要修改为每个角色候选都对应一个待采纳按钮
11. 发送消息后，模型执行期间，打开作品档案，作品档案没有显示内容
12. 我发送“主角设定” ，应用返回了“当前作品已有 1 个已确认角色：埃德温·暮影。这次只是读取角色列表，没有写入作品事实。 ”
13. 我发送“主角是谁，叫啥 ” ，应用返回了“当前作品已有 1 个已确认角色：埃德温·暮影。这次只是读取角色列表，没有写入作品事实。 ”
14. 记忆页面，列表显示数据字体颜色、行颜色，等等，需要从 UX 的角度分析重构，要符合全局调性
15. 模型供应商，需要添加 openai （api_key、subscription两种认证方式）、minimax、智谱、kimi、gemini
16. 记忆，需要区分不同类型的记忆，尤其是对于“角色记忆”这个类型的记忆，需要进一步明确和规范化
17. 记忆，需要澄清并梳理写入的时机以及内容，什么时候写入，什么时候不写入，写入的内容是什么，写入的内容是否需要进行后续处理和更新、移除废弃等，确保记忆是对作品创作有帮助的是正面的影响，而不是冗余的或者过时的

18. 思考中时，打开阅读模式，内容是空的
19. 阅读模式下，小说内容应该是连贯的滑动到下一章，而不需要点击每章的章节名，并且在滑动时章节名有选中效果
20. 作品档案的角色卡信息以及经验规则信息内容，排版不清晰，全都挤在一坨
21. 页面布局改为三栏，左侧为会话列表，中间为对话流，右侧为作品档案