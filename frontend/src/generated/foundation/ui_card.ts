import { z } from "zod"

export const UiCardSchema = z.object({ "card_type": z.enum(["candidate_set","confirmation_card","result_card"]).describe("信息卡片类型，集合由 ADR-0024 决策 3 冻结。"), "priority": z.string().describe("展示优先级提示（high / normal）。").optional(), "visibility": z.string().describe("可见性提示（always 等）。").optional(), "title": z.string().describe("作者可见卡片标题。").optional(), "body": z.string().describe("作者可见卡片正文。").optional(), "artifact_refs": z.array(z.string()).describe("candidate_set：关联 tentative artifact set 引用。").optional(), "candidate_set_ref": z.string().describe("candidate_set：候选集引用。").optional(), "artifact_type": z.string().describe("candidate_set：artifact 类型（prose_fragment / character_seed / ...）。").optional(), "items": z.array(z.record(z.string(), z.any())).describe("candidate_set：候选条目。item 字段与 Provider 响应字节一致（I1 因果绑定），此处不收紧内部形状。").optional(), "tentative": z.boolean().describe("candidate_set：是否待采纳（tentative-first）。").optional(), "revision_of": z.union([z.string().describe("candidate_set（修订草稿）：原草稿引用。"), z.null().describe("candidate_set（修订草稿）：原草稿引用。")]).describe("candidate_set（修订草稿）：原草稿引用。").optional(), "revision_reason": z.union([z.string().describe("candidate_set（修订草稿）：修订原因。"), z.null().describe("candidate_set（修订草稿）：修订原因。")]).describe("candidate_set（修订草稿）：修订原因。").optional(), "quality_finding_refs": z.array(z.string()).describe("candidate_set（修订草稿）：针对的质量发现引用。").optional(), "behavior_ref": z.string().describe("confirmation_card：关联 BehaviorState 引用。").optional(), "target_ref": z.string().describe("confirmation_card：确认对象引用。").optional() }).strict().describe("ui_cards 信息通告卡片（ADR-0024 决策 2/3）。仅承载信息通告，不携带可提交动作（N-SURF：禁止 actions 字段，additionalProperties=false 使漂移在校验层变红）。card_type 现行集合为 candidate_set / confirmation_card / result_card；新增卡片必须先修订 ADR-0024 与 07 §4.2。")
export type UiCard = z.infer<typeof UiCardSchema>
