import { z } from "zod"

export const ArtifactAdoptionEntrySchema = z.object({ "artifact_id": z.string(), "artifact_type": z.string().describe("Foundation artifact 类型或 Domain 注册类型。"), "adoption_status": z.enum(["TENTATIVE","ACCEPTED","EDITED_ACCEPTED","DISCARDED","SUPERSEDED","INVALIDATED","ARCHIVED"]).describe("由 30 §3.2 冻结的 7 态。本 ADR 直接内联，因为 30 §3.2 已是 canonical 唯一权威。"), "requires_adoption": z.boolean().describe("canonical 名称由 30 §3.1 冻结，不再使用 adoption_required。"), "revision_base": z.union([z.string().describe("artifact 生成时依赖的源 revision（30 §2.2）。adoption 前必须与当前 target scope revision 比较。"), z.null().describe("artifact 生成时依赖的源 revision（30 §2.2）。adoption 前必须与当前 target scope revision 比较。")]).describe("artifact 生成时依赖的源 revision（30 §2.2）。adoption 前必须与当前 target scope revision 比较。").optional(), "supersedes_artifact_id": z.union([z.string().describe("若 adoption_status = SUPERSEDED，指向取代它的 artifact id。"), z.null().describe("若 adoption_status = SUPERSEDED，指向取代它的 artifact id。")]).describe("若 adoption_status = SUPERSEDED，指向取代它的 artifact id。").optional() }).catchall(z.any())
export type ArtifactAdoptionEntry = z.infer<typeof ArtifactAdoptionEntrySchema>
