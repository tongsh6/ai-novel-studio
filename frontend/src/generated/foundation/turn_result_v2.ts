import { z } from "zod"

export const TurnResultV2Schema = z.object({ "schema_version": z.string().regex(new RegExp("^\\d+\\.\\d+\\.\\d+$")).describe("TurnResult schema 版本，semver。本 ADR 冻结的版本是 2.0.0。"), "turn_id": z.string().describe("本轮 turn 的稳定唯一 id，由 Orchestrator 生成。"), "task_id": z.union([z.string().describe("若本 turn 关联 long-run task，则填该 task id；否则 null。"), z.null().describe("若本 turn 关联 long-run task，则填该 task id；否则 null。")]).describe("若本 turn 关联 long-run task，则填该 task id；否则 null。").optional(), "agent_id": z.union([z.string().describe("产出本 turn 的 Agent id。Orchestrator 直接处理时填 orchestrator 的 agent_id；Multi-Agent 场景下子 Agent 必须填自身 id；纯系统内部 turn 可填系统保留 id 或 null。"), z.null().describe("产出本 turn 的 Agent id。Orchestrator 直接处理时填 orchestrator 的 agent_id；Multi-Agent 场景下子 Agent 必须填自身 id；纯系统内部 turn 可填系统保留 id 或 null。")]).describe("产出本 turn 的 Agent id。Orchestrator 直接处理时填 orchestrator 的 agent_id；Multi-Agent 场景下子 Agent 必须填自身 id；纯系统内部 turn 可填系统保留 id 或 null。").optional(), "parent_turn_id": z.union([z.string().describe("若本 turn 是 child agent 在 parent turn 内的子轮，则填 parent turn id。"), z.null().describe("若本 turn 是 child agent 在 parent turn 内的子轮，则填 parent turn id。")]).describe("若本 turn 是 child agent 在 parent turn 内的子轮，则填 parent turn id。").optional(), "phase": z.any().describe("完整枚举由 ADR-0002 冻结。语义见 02 §4.1 / §5.1。"), "status": z.any().describe("完整枚举由 ADR-0002 冻结。语义见 02 §4.4 / §5.2；status family 为 turn / task / artifact projection 共用。"), "next_action": z.any().describe("完整枚举由 ADR-0002 冻结。语义见 01 §9.3 / 02 §11；UI 映射见 30 §9。"), "assistant_message": z.any().describe("顶层主消息。canonical 名称为 assistant_message，不再使用 main_message / primary_message 等别名。结构见 11 §4.1，详细 schema 由后续 ADR 冻结。"), "ui_cards": z.array(z.any()).describe("顶层结构化卡片列表，与 assistant_message 是两类并列顶层呈现对象（11 §4）。"), "behavior_state": z.object({ "active": z.union([z.object({ "behavior_type": z.string().describe("durable behavior 名称。canonical 集见 30 §6.1：clarification / confirmation / correction / cancellation。"), "behavior_id": z.string().describe("本 behavior 实例 id。"), "status": z.any().describe("完整枚举由 ADR-0002 冻结。"), "resolution_ref": z.union([z.string().describe("behavior 已 resolve 时的 resolution 引用；durable 必须有，instant 不强制（30 §6）。"), z.null().describe("behavior 已 resolve 时的 resolution 引用；durable 必须有，instant 不强制（30 §6）。")]).describe("behavior 已 resolve 时的 resolution 引用；durable 必须有，instant 不强制（30 §6）。").optional() }).catchall(z.any()).describe("当前未关闭的 durable behavior 状态；instant behavior 不出现在 active。"), z.null().describe("当前未关闭的 durable behavior 状态；instant behavior 不出现在 active。")]).describe("当前未关闭的 durable behavior 状态；instant behavior 不出现在 active。"), "history": z.array(z.object({ "behavior_type": z.string(), "behavior_id": z.string(), "status": z.any(), "resolution_ref": z.union([z.string(), z.null()]).optional() }).catchall(z.any())).describe("本 turn 内已结束（含 instant 完成）的 behavior 摘要列表，按发生顺序。") }).strict().describe("本轮活跃 behavior 的快照。durable / instant 分类见 30 §6。"), "adoption_state": z.object({ "pending": z.array(z.any()).describe("本 turn 产出且尚未达终态的 artifact 列表。"), "resolved": z.array(z.any()).describe("本 turn 内进入终态（ACCEPTED / EDITED_ACCEPTED / DISCARDED / SUPERSEDED / INVALIDATED / ARCHIVED）的 artifact 列表。") }).strict().describe("本轮新产生或状态变化的 artifact adoption 视图。adoption 7 态由 30 §3.2 冻结。"), "projection_refs": z.array(z.object({ "projection_type": z.string().describe("投影类型；Domain 注册，例如 reading_projection_root / reading_projection_toc / reading_projection_chapter / reader_recap（30 §8.3）。"), "projection_id": z.string(), "source_revision_refs": z.array(z.string()).describe("该投影所依赖的 accepted / authoritative source revisions（30 §2.3）。"), "refresh_status": z.union([z.string().describe("投影刷新状态；具体枚举由 §29.7.1 第 11 项 ADR 冻结。"), z.null().describe("投影刷新状态；具体枚举由 §29.7.1 第 11 项 ADR 冻结。")]).describe("投影刷新状态；具体枚举由 §29.7.1 第 11 项 ADR 冻结。").optional() }).catchall(z.any())).describe("本 turn 影响到的派生投影引用。canonical 名称为 projection_refs（与 30 §2.3 source_revision_refs 对齐复数命名风格）。Domain 投影对象族定义在 30 §8.3。"), "validation": z.any().describe("ValidationEnvelope。详细 schema 由独立 ADR 冻结；本 ADR 仅声明字段引用与必填性。"), "usage": z.any().describe("UsageEnvelope。token / 时间 / 成本 / 写入规模等度量；详细 schema 由独立 ADR 冻结。"), "trace_ref": z.any().describe("TraceRef。指向本 turn 的 trace；详细 schema 由独立 ADR 冻结。"), "warnings": z.array(z.any()).describe("本 turn 产生的非阻塞警告。").optional(), "errors": z.array(z.any()).describe("本 turn 产生的错误。errors 非空时 status 不得为终态成功类值（如 02 §5.2 中的 DONE），具体禁止集合由 ADR-0002 联调。").optional(), "produced_at": z.string().datetime({ offset: true }).describe("本 TurnResult 生成时间，ISO 8601 / RFC 3339。") }).strict()
export type TurnResultV2 = z.infer<typeof TurnResultV2Schema>
