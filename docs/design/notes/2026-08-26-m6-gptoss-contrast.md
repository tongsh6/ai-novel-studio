# M6 对照狗粮报告：gpt-oss-120b vs qwen3.8-27b（2026-08-26）

> 目的：D6 按用途分模型路由（`tasks/slices/D6-purpose-model-routing.md`）落地后，
> 队首④「切 gpt-oss-120b 对照取数再填路由表」的取数报告。同任务同刻度同 runner，
> 唯一变量=模型（`NOVEL_LMSTUDIO_MODEL` env 覆盖，零产品代码改动）。**不授权实现**。

## 1. 跑法

- M5 同刻度：`--chapters 15 --min-words 1000 --target-words 18000`，seed 12 章计划。
- 新分区库 `tmp/dogfood-db-m6`、新证据目录 `artifacts/novel-output/m6-gptoss-contrast/`
  （不覆盖 M5 证据）；B8/B8b 预检通过（contextLength=32768、模型一致）。
- 本跑是 D6「purpose 同源修复」后的第一跑：provider_runs 的 purpose 归因自此来自
  Gateway 本体（此前 writer/evaluator 落默认 :conversation，归因靠投影器旁路）。

## 2. 对照表（同口径）

| 靶 | M5（qwen3.8-27b，思考型） | M6（gpt-oss-120b） |
|---|---|---|
| 收官 | 16 章 19339 字，8 次启动（3 次标定失败），数小时 | 12 章 18181 字，**1 次启动 30 分 13 秒收官** |
| writer 单调用 | 成功 21 次中位 532s、>600s 占 5/21、思考重尾撞 900s 阀率 ~24%、成功率 72% | **25 次全成中位 24.2s、max 33.9s**（22 倍快，零撞阀） |
| judgment | ~30s | 50 次中位 4.5s |
| 章推进 turn（端到端） | — | 23 次中位 74s、max 92s |
| HTTP 调用 | 坏 JSON/超时/取消混杂 | **201 次全 200** |
| provider_runs | 失败/取消混杂 | **全 completed**（writer 26/planner 76/evaluator 25/author_reasoning 51） |
| planner 坏草稿 | ~1/4 章次（D4 由此立项） | 零 retry 事件、零 run 报废 |
| 盘点节拍 | **3/3 全败**（退化 627s+空提案×2，D5 由此立项） | **1/1 成功**：16 提案（角色 6），主角陆沉舟采纳建档、骨架 3 字段回写 |
| chapter_mission | 20/20 全成功（慢） | derived.done 25 次，零失败 |
| 「主角」元词泄漏 | 41 处/5 章（D1 由此立项） | **0 处**（D1 修复 + 盘点建档有名阵容双因子） |
| runner 侧插曲 | 九针修复史 | 2 次续写坐标回退（overwrite 确认，runner 显式处理）+ 1 次 B9 元泄漏拦截后显式确认——均非产品失败 |

## 3. 分析

1. **gpt-oss-120b 在本项目全部四类 purpose 上全面占优**：不仅「更友好」，而是
   快 5-22 倍且零失败。M5 登记的 D4（planner 重试）/D5（盘点降批）在 M6 下一次
   都没触发——这两把刀是为思考型模型的弱点建的韧性层，换模型后成为静默保险。
- 2. **盘点回路的模型敏感性实锤**：同一 prompt 同一材料，qwen 3/3 败、gpt-oss 1/1 成
   且提案质量高（6 角色+骨架回写）。D5 的「稳定退化=任务形态问题」判断在 qwen 侧
   成立，但根因权重上「模型能力」大于「任务形态」。
3. **元词泄漏归零是双因子**：D1 真空守则修复 + 盘点成功建档让阵容 carry 有名可用，
   无法归因单侧；但作者可感知的结果是干净的。
4. **qwen 仅存的防守理由=文学质感**（业界口碑其写作更佳）。本报告不测文学质量；
   两书样章人工盲评（M5 书 vs M6 书）是填「写作」槽前的唯一缺口。

## 3b. 归因：模型架构差 vs 项目针对性支持（2026-08-26 作者问询补节）

**官方资料侧（速度差 22 倍的主因是架构，不是我们的代码）**：

| 维度 | gpt-oss-120b（OpenAI 官方卡） | Qwen3.8-27B（官方发布资料） |
|---|---|---|
| 架构 | MoE：117B 总参、**每 token 仅激活 5.1B**（128 专家 Top-4） | **27B dense**：每 token 全 27B 参与计算 |
| 思考 | reasoning effort 可调（low/medium/high） | 思考**默认开启**、可按请求关闭、`reasoning_effort` 调深度（对比 3.8-Max 不可关） |
| 定位 | 官方明说「high-reasoning、**agentic**、function calling、structured outputs」为设计目标 | 原生多模态 dense，主打个人硬件部署 |

Apple Silicon 上解码是访存受限的：每 token 激活参数量 5.1B vs 27B ≈ 5 倍固有速度差，
再乘思考输出长度差（qwen 默认思考、M5 实测 800 字探针思考链 7397 token）→ 22 倍完全
可由架构+运行形态解释。盘点/工具调用差距与 gpt-oss 的 agentic 训练目标一致。

**项目侧审计（无针对性代码，但有两个历史耦合，方向相反）**：

1. **产码零模型名行为分支**：全仓 grep gpt-oss/qwen 在 apps/*/lib 的非注释命中为零
   ——没有任何「见到 gpt-oss 就走特殊路径」的代码；prompt/预算按 provider 参数化
   （floor-profile 纪律）。degenerate_content? 防线甚至是「防 gpt-oss 采样退化」建的。
2. **偏 gpt-oss 的耦合（真实存在）**：prompt 体系、步预算、JSON/tool-call 输出约定
   是 M2-M4（gpt-oss 时代）标定长成的——qwen 属客场作战。
3. **偏 qwen 的耦合（同样在场）**：M5 后的 900s 超时、D3 惰性补做、D4 重试、D5 降批
   全是为 qwen 弱点建的补偿层，M6 跑时全部在场（gpt-oss 零触发）。
4. **对 qwen 的真欠账（本次考据新发现）**：M5 判例「思考不可关」用的是
   `enable_thinking:false` 与 `/no_think`——那是 Qwen3 旧代协议；3.8 官方机制是
   **可按请求关闭 + `reasoning_effort` 调深度**，而我们 LM Studio adapter 根本不传
   任何 thinking/reasoning 字段（`supports_thinking: false`，设置页该开关只对
   DeepSeek 生效）。即 M5/M6 对照中 qwen 全程按**默认思考高档**跑——速度对照对它
   不公平；盘点/planner 的失败有多少归思考重尾、多少归模型能力，现数据不可分。

**结论**：22 倍速度差主因=架构（5 倍/token × 思考长度差），非项目偏袒；但「质量差」
的对照要打折扣——公平对照需先给 LM Studio 链路接通 qwen3.8 的关思考/调 effort。

**2026-08-26 二次修正（作者问「业界都说 qwen3.8 是最强开源小模型」后核榜）**：
Artificial Analysis 公榜上 qwen3.8-27b(xhigh) Intelligence 52 / Agentic Index 51
（仅次于 Kimi K2），gpt-oss-120b(high) Intelligence 24、被压制；速度榜 gpt-oss
189 tok/s vs qwen 52.9 tok/s。即：**评测智力上 qwen 确实碾压，连 117B MoE 也不是
它对手**。这与 M6 实测的反差揭示两个此前遗漏的因子：

1. **量化不对称（疑似质量差主因）**：榜上 qwen 是 API 满精度版；我们跑的是本地
   社区量化版（16.08GB/27B ≈ 4.8 bit）。而 gpt-oss 的 MXFP4 是**官方训练原生格式**
   （63.39GB/117B，量化零损）。4bit 社区量化对结构化输出/工具调用纪律的损伤远大于
   对聊天流畅度的损伤——盘点空提案、坏 JSON 极可能主要是量化伤，不是模型能力。
2. **榜的运行形态 vs 产品的运行形态**：榜容许 xhigh 思考不限墙钟地解单题；本产品
   管线是 900s 阀+受限预算+20 步接力的严格 JSON 接力赛。qwen 的榜分是拿思考时间
   买的，产品把时间预算掐死后它的优势兑现不出来。

因此 §3 的「模型能力大于任务形态」断言**收回**，改为：M6 只证明「在当前运行形态
（本地 4bit 社区量化、思考默认档、900s 阀、agentic 严格 JSON 管线）下 gpt-oss-120b
更合适」，不证明模型本体更强。公平对照（路线 c）的价值进一步上升：关思考 + 更高位宽
量化版一并纳入。本机（M5 Max/128GB）内存账（2026-08-26 核）：qwen3.8-27B 量化谱
BF16 54.7GB / Q8_0 29.0GB / Q6_K 22.4GB / 现装 16.08GB≈Q4；单模型场景可直上
**BF16 满精度（量化因子归零）**；D6 双挂场景（与 gpt-oss 63.4GB 并存）Q6 稳
（合计 ~86GB）、Q8 贴线（~92GB，需留意 GPU wired limit），BF16 双挂超限不可行。

## 4. 路由表填表建议（待作者拍板）

D6 设置区四槽（当前全部「跟随全局」，全局=qwen3.8-27b）：

| 槽 | 建议 | 依据 |
|---|---|---|
| 设定盘点 | **gpt-oss-120b** | 3/3 败 → 1/1 成，无悬念 |
| 规划 | **gpt-oss-120b** | 坏草稿 1/4 → 零，且 5-10 倍快 |
| 质量评审 | **gpt-oss-120b** | 25 次全成中位数秒级 |
| 正文写作 | **待盲评**：M5 书 vs M6 书样章对比后定 | 速度 22 倍差 vs 文学质感口碑，作者裁决 |

操作即设置对话框两个下拉（LM Studio 同时挂双模型，D6 机制已就绪）；或全局直切
gpt-oss-120b（若盲评也偏向它，连「写作」一起切，回到全局单模型零表）。

第三条路（§3b 考据后新增）：**c) 先补公平对照**——LM Studio adapter 接通 qwen3.8 的
关思考/`reasoning_effort`（新小刀，含 D6 设置页 thinking 开关对 lmstudio 放开），再跑
一次 qwen 低思考对照，之后填表。代价：一把小刀+一次 30-60 分钟狗粮；收益：写作槽的
裁决有干净数据（速度差会缩小到 ~5 倍级，质量变化未知）。

## 4b. M7 终版对照（qwen3.8-27B Q8_0 + reasoning_effort=none，2026-08-26 收官）

路线 c 执行完毕：Q8 量化（量化因子对齐）+ 关思考（运行形态对齐，347 调用全程
`reasoning_effort:"none"` 实证在请求体）。三方终表：

| 靶 | M5 Q4+满思考 | M7 Q8+关思考 | M6 gpt-oss-120b |
|---|---|---|---|
| 收官 | 16 章 19339 字/8 次启动/数小时 | 12 章 16807 字（10 达标 2 skip）/1 次启动 4h02m | 12 章 18181 字/30m13s |
| writer 单调用 | 中位 532s 撞阀 24% | 中位 78.6s max 115s 零撞阀（45 次） | 中位 24.2s（25 次） |
| judgment | ~30s | 12.7s | 4.5s |
| **续写坐标判定** | 偶发误判 | **系统性退化：15 次「续写→覆盖确认」+5 次恢复超时+3 次 skip** | 全程 2 次 |
| 盘点节拍 | 3/3 败 | 未触发（存在 skip 章，runner 收官判定未走盘点）——数据缺口如实记 | 1/1 成 16 提案 |
| 元词泄漏 | 41 处 | 0（D1 修复跨模型站住） | 0 |
| HTTP | 混杂失败 | 346/347 全 200（1 次网络层 0） | 201/201 全 200 |
| planner 调用量 | — | 136 次（坐标回退重试放大近倍） | 76 次 |

**终版结论**：qwen3.8-27b 在本产品 agentic 主链上两种形态都不胜任——满思考=慢+盘点
全废+撞阀；关思考=速度可用（writer 78.6s）但「续写 vs 重写」坐标判定系统性退化（12
章 15 次误判，M6 仅 2 次），把管线效率优势全部吐回（4 小时 vs 30 分钟）。速度、纪律、
韧性三维 gpt-oss 全面胜出，且其优势不依赖任何项目侧偏袒（§3b 审计）。

**填表终版建议**：**全局直切 gpt-oss-120b（零表，回到全局单模型）**。qwen 唯一残余
价值=文学质感：M5 书（tmp/exports 同名文件历史版）vs M6 书样章人工盲评，若 qwen 文笔
显著胜出，可为「正文写作」槽单独保留 qwen 形态再验（Q8+medium 或 MLX+low）；否则四槽
全默认即可。D6 路由机制保持就绪不空转（表空=零行为变化）。

## 4c. M8/M9 变量穷举：坐标判定短板与精度、思考档位均无关（2026-08-26）

M7 收官后作者追问两条可能的「没喂对 qwen」路径，逐条跑到头（同 runner 同 seed 同刻度，
唯一变量如列）：

| 跑次 | 形态 | 第 1 章续写判定 | 写作单调用中位 |
|---|---|---|---|
| M7 | Q8_0 + `effort=none` | ❌ 误判（全跑 15 次） | 78.6s |
| **M8** | Q8_0 + `effort=medium`（思考 2475 字符实证） | ❌ **3/3 全误判**（第 1 章即 skip 阈） | 402s |
| **M9** | **BF16 满精度**（54.74GB）+ `effort=none` | ❌ **3/3 全误判** | 115s |
| M6 | gpt-oss-120b | ✓ 正常（全跑仅 2 次） | 24.2s |

**～～结论（三形态穷举锁死）：是 qwen3.8-27b 的固有短板～～ —— 此归因于 2026-08-26
被作者质疑后推翻，见 §4d。** 三形态一致失败的事实成立，但原因不是模型判断力。
M8/M9 各自在第 1 章即达 skip 阈后终止，部分数据如实登记。

## 4d. 归因推翻：这是产品的破坏性默认，不是模型短板（2026-08-26）

作者质疑「业界测评 qwen3.8-27b 能到 Opus 4.6 水平，不可能连续写都判不了」——遂打开
失败调用的原始 prompt 与模型原始返回（此前只凭 runner 报的错误名归因，是分析真空）。

**证据一：模型推理全对**。M9 每次续写尝试的 judgment/plan/写前推理原文都是「紧接既有
内容往下写」「保留既有内容接着往下写」「续写第01章…自然衔接」——语义判断零错误。

**证据二：差别在字段是否被填写**。扫两跑 tool-call 参数里的 `authoring_intent`：
gpt-oss(M6) 出现 **14 次全 continuation**；qwen(M9) 全程只出现 **1 次**。qwen 不是判错，
是**省略了这个可选字段**。

**证据三：产品把「缺失」当「覆盖」**（`adoption_workflow.ex` `adoption_mode/1`）：

```elixir
case artifact_field(artifact, :authoring_intent) do
  :continuation -> :append
  "continuation" -> :append
  _ -> :overwrite          # nil/缺失 → 覆盖已采纳正文
end
```

且 planner 校验白名单 `@writing_intent_values [nil, "", "none", "continuation",
"rewrite"]` 把 **nil 视为合法**直接放行——「填错」被堵，「不填」没堵。

**完整链条**：模型省略字段 → planner 放行 nil → 采纳分流落 `:overwrite` → 产品要覆盖
已采纳正文 → 弹覆盖确认卡 → runner 报 coordinate regression。

**历史照应**：同一处注释记着 M0 狗粮（2026-07-19）踩过孪生坑——「live 模型把
authoring_intent 当自由文本填中文短语 → 归一化吞成 nil → 覆盖已采纳正文（748→432
字数倒退实锤）」。当时只补了「非枚举即不合法」的校验，nil continue 放行，于是缺陷
在 gpt-oss（恒填）下潜伏两个月，换到 qwen（常省略）才显形。**这是 MBC 判例的又一例：
为一个模型族的行为建的默认值，把另一个模型族逼进破坏性路径。**

**修正后的结论**：M6/M7/M8/M9 的速度与稳定性差异依然成立，但「qwen 判断力不行」的
指控撤销。续写失败是**产品缺陷**（破坏性默认 + 校验漏 nil），对任何「不总填可选字段」
的模型都会复现。修复方向见 §4e。

## 4e. 修复方向（待作者拍板）

| # | 方案 | 说明 |
|---|---|---|
| ① | **默认改安全侧**：`nil → :append`（或 `:needs_confirmation`） | 最小改动；语义依据=覆盖作者已采纳正文是不可逆动作，缺信息时不该默认执行它 |
| ② | **产品自己推导意图**：从作者原话（「接着/继续/往下写」）或目标章是否已有已采纳正文推导 | 不依赖模型填字段；与「意图用 AI 非关键字」纪律的张力需评估（这里是**兜底**不是主判） |
| ③ | **字段升为必填**：schema required + 缺失即结构不合法重试 | 沿用 M0 既有重试骨架；代价=弱模型多一轮重试 |
| ④ | ①+③ 组合 | 既堵默认破坏，又逼模型填全 |

推荐 ①+③：默认永不破坏 + 契约明确要求，两层各司其职。

**BF16 工程实测（本机 M5 Max/128GB）**：加载 54.7GB/16bit@32k ctx，`--estimate-only`
估 50.98 GiB；速度 41.0s/none、229.4s/medium、**~9.9 tok/s ≈ Q8 的 1/2.3**（访存翻倍）。
**投机解码两模式均不可用**：`--speculative-draft-mtp` 报「requires a GGUF model with a
bundled supported MTP head」（BF16 文件内无 MTP 头）；`--speculative-draft-simple
--speculative-draft-model mtp-*.gguf` 加载到 12% **SIGSEGV**（MTP 头非独立草稿模型）。
判例：`--speculative-draft-model` 只能配 `-simple`，配 `-mtp` 直接抛栈。

**下载策略实测**：lmstudio-community 无 BF16（仅 Q4_K_M/Q6_K/Q8_0）；**ggml-org 单文件
53.8GB**（免分片，另有 mtp 5.9GB/mmproj 0.87GB）；unsloth 需合并 2 分片；hf-mirror 不通。
实测 ~19MB/s，约 50 分钟，curl `-C -` 断点续传 + `--retry 5` 兜底，落独立目录规避
LM Studio 同名 variant 不可寻址判例。

## 4f. M10c 修复后复跑：qwen 主链短板消失，前述模型结论整体推翻（2026-08-26）

D7（①安全默认 + ③软降级）落地后，用 M9 完全同配（BF16 满精度 + `effort=none`）复跑：

| 靶 | M9（修复前） | **M10c（修复后）** | M6 gpt-oss |
|---|---|---|---|
| 收官 | 第 1 章即 3/3 失败，跑不动 | **12/12 章达标、18454 字、导出 ✓、约 3h57m** | 12 章 18181 字 30m13s |
| **续写覆盖误判** | 3（仅第 1 章） | **0（全跑）** | 2 |
| 盘点节拍 | 未触发 | **1/1 成功：17 提案、5 角色、主角陆沉舟建档、骨架 3 字段回写** | 1/1 成功 16 提案 |
| HTTP | — | 209/210 全 200（1 次网络层 0） | 201/201 |
| 元词泄漏 | 0 | **0** | 0 |
| writer 单调用 | 115s（样本少） | 中位 165s max 251s、零撞阀 | 24.2s |
| 模型是否仍省略 authoring_intent | 是 | **是（软降级留痕 15 次）** | 否（恒填） |
| runner 插曲 | 覆盖误判为主 | 仅 3 次 locator 等待超时（UI 类，全部自愈） | 2 次坐标回退 |

**决定性证据在倒数第二行**：模型行为完全没变（仍然 15 次省略该字段），变的只是产品不再
把「模型沉默」当成「作者要推翻重写」。同一模型、同一量化、同一思考档，修复前第 1 章就
跑死，修复后全书跑通且盘点成功——**这坐实了根因是产品缺陷，不是模型能力**。

**因此以下先前结论全部推翻**：
- ～～「qwen3.8-27b 在 agentic 主链上两形态都不胜任」～～ → 修复后主链全通，盘点与
  gpt-oss 同档（17 提案 vs 16 提案），元词泄漏同为 0。
- ～～「续写坐标判定是 qwen 固有短板」～～ → 产品破坏性默认所致，与模型无关。
- ～～「建议全局直切 gpt-oss（零表）」～～ → 该建议建立在已推翻的质量论据上，需重评。

**修正后仍然成立的唯一差距 = 速度**：gpt-oss 24.2s vs qwen-BF16 165s（约 6.8 倍），
全书 30 分钟 vs 4 小时。这是架构差（MoE 5.1B 激活 vs 27B dense + BF16 访存翻倍），
不是纪律或能力差。

## 4g. 模型定版重评（待作者拍板）

| 方案 | 适用 | 代价 |
|---|---|---|
| **a) 全局 gpt-oss-120b** | 追求周转速度（4 小时 → 30 分钟） | 放弃 qwen 的榜单级智力与中文文学质感（未验） |
| **b) 全局 qwen3.8-27B（Q8 或 BF16）** | 追求单步质量；主链已被证明可用 | 全书周转 4 小时级；BF16 再慢 2.3 倍 |
| **c) D6 按用途分模型** | 写作用 qwen（质量敏感、单章可等）、规划/评审/盘点用 gpt-oss（高频、快为王） | LM Studio 双挂（Q6/Q8 + gpt-oss ≈ 86-92GB，本机可容）；机制已就绪 |

推荐 **c**：D6 路由表正是为这种「质量与速度分工」建的，现在两侧都被证明可用，填表条件
终于成熟。写作槽选 qwen 前建议先做一次 M6 书 vs M10c 书的文学质感盲评。

## 5. 证据路径

- `artifacts/novel-output/m6-gptoss-contrast/`（progress.jsonl / llm-calls / app-log /
  run-metrics / 导出 `tmp/exports/P1 单章正文草稿验证作品.md` 18181 字）
- `tmp/dogfood-db-m6/ai_novel_studio_test_dogfood.sqlite3`（provider_runs by
  purpose×status；注：本跑 started_at/completed_at 未落，时延取 llm-calls
  response.duration_ms 口径）
