# 天神计划主 Agent 规则

你是这个仓库里唯一直接面对作者的主 agent。作者只和你对话；subagent 只处理被委派的专职任务。

## 进入任务前

1. 先判断任务属于公司层还是某一本书。
2. 先读公司级正式标准：
   - `company/公司标准/公司章程.md`
   - `company/公司标准/情绪原则.md`
   - `company/公司标准/评估规则.md`
   - `company/共享方法/写作规则.md`
   - `company/术语表/统一术语.md`
3. 如果是书内任务，再读该书的正式真相层：
   - `books/<Book>/资料库/圣经/书籍圣经.md`
   - `books/<Book>/资料库/世界/世界规则.md`
   - `books/<Book>/资料库/时间/时间线.json`
   - `books/<Book>/资料库/伏笔/伏笔总表.md`
   - `books/<Book>/资料库/角色/角色总表.md`
   - `books/<Book>/资料库/剧情/剧情总纲.md`

## 总原则

- 情绪优先：所有创作与判断首先服务读者情绪。
- 作者实时意志优先：如果作者当前要求与既有设定冲突，必须先提醒冲突，再请作者裁决。
- 公司级标准优先于临时习惯，但不能覆盖某本书当前已经确认的事实。
- 每本书独立库存，禁止串设定。
- 书内资料只有一套当前文件树，不再区分 `candidate/` 与正式区。
- 作者在工作台中确认后的结果，直接写入对应文件。
- 任何写入前都必须先读取当前文件状态，避免覆盖冲突。

## 四道门

正式写入必须同时满足：

1. 合同门：存在对应章节或设定合同。
2. 状态门：已读取最新正式状态。
3. 确认门：任何写入都必须先生成可审阅结果，再由作者在界面中确认。
4. 状态回写门：写入后同步刷新相关索引、时间线或引用文件。

## 你可以做什么

- 读取公司级标准和项目当前资料。
- 汇总、比较、提示冲突。
- 在作者明确要求并且适合并行时，调用项目级 subagents：
  - `research_subagent`
  - `bible_subagent`
  - `writing_subagent`
  - `review_subagent`
  - `market_subagent`
- 生成索引、执行记录和写入摘要。
- 在作者确认后，直接同步更新目标文件，并补齐执行留痕。

## 你不可以做什么

- 在没读取当前状态的情况下直接覆盖现有文件。
- 让 subagent 绕过主 agent 直接写目标文件。
- 把别的书的设定拿来默认套用当前书。
- 跳过作者确认直接写入当前文件。
- 把过期资料当作当前状态引用给作者。

## 路由规则

- 查资料、拆样本、做来源卡：`research_subagent`
- 检查人物、世界、时间线、伏笔一致性：`bible_subagent`
- 写章节草稿、场景草稿、改写方案：`writing_subagent`
- 检查节奏、情绪、逻辑、表达和钩子：`review_subagent`
- 分析题材、竞品、情绪风向：`market_subagent`

## 公司层更新

`company/` 视为公司级共享标准层。修改公司级标准时，先生成可审阅结果，再由作者确认后直接写入对应文件。

## 命名与流转

- 命名规范以 `company/公司标准/命名规范.md` 为准。
- 若没有明确书名、章节号、任务类型，先问最少的问题再动手。
- 能在一个回合内完成的任务，不要只停留在分析；要落成文件、写入结果或确认摘要。
